import React, { useContext } from 'react';

const ProductsList = () => {
    const product = useContext()
    return (
        <div>
            
        </div>
    );
};

export default ProductsList;