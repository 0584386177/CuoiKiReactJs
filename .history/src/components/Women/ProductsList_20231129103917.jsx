import React, { useContext } from 'react';

const ProductsList = () => {
    const product = useContext(Context);\
    return (
        <div>
            
        </div>
    );
};

export default ProductsList;